# 2026-03-28 15:50 — 기존 Claude 환경 머지 검증 및 수정

## 검증 대상
`C:/Users/USER/Documents/ModularStage/.claude` — 실제 운영 중인 UE5 프로젝트

## 발견된 기존 환경 구조
```
ModularStage/
├── CLAUDE.md                      ← 루트 CLAUDE.md (프로젝트 개요)
├── .mcp.json                      ← 루트 레벨 MCP 설정 (절대 경로)
└── .claude/
    ├── CLAUDE.md                  ← 상세 UE5 규칙 파일 (팀 규칙 보관)
    ├── settings.local.json        ← 로컬 설정 (MCP 3개 + permissions)
    └── mcp/
        ├── gemini-cli.js
        └── uelog_analyzer.py
```
- `settings.json` 없음 (로컬 설정만 `settings.local.json`으로 관리)
- 기존 MCP: `gemini-cli`, `uelog-analyzer`, `unreal-mission-editor`

## 머지 시뮬레이션 결과

| 항목 | 결과 | 비고 |
|------|------|------|
| 루트 `CLAUDE.md` | ✅ AgentWatch 섹션 끝에 추가 | 기존 내용 보존 |
| `.claude/settings.local.json` | ✅ 건드리지 않음 | 기존 MCP 3개 보존 |
| `.claude/settings.json` | ✅ 신규 생성 (MCP 4개) | 기존 MCPs와 이름 충돌 없음 |
| `.claude/agents/` | ✅ 새로 생성 | |
| `.mcp.json` | ✅ 건드리지 않음 | |

## 발견된 문제
`.claude/CLAUDE.md`가 존재할 때 AgentWatch 안내가 루트 CLAUDE.md에만 추가됨.
팀이 `.claude/CLAUDE.md`에 상세 규칙을 관리하는 패턴을 쓰는 경우,
AgentWatch 내용이 거기서도 보여야 혼란이 없음.

## 수정 내용 (`watch.py`)
- `_update_project_claude_md()` 리팩터링
  - 공통 로직 → `_merge_claude_md_section(md_path)` 헬퍼로 분리
  - 루트 CLAUDE.md: 항상 처리 (기존 동일)
  - `.claude/CLAUDE.md`: 파일이 **존재하는 경우에만** 마커 구역 추가/갱신 (신규)
  - 없는 경우 새로 생성하지 않음 (불필요한 파일 생성 방지)
