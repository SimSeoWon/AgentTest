# AgentWatch 종합 분석 보고서

**날짜**: 2026-04-11
**분석 방법**: 엔진팀 / 클라이언트 개발자 / QA / TD 4개 관점 병렬 분석
**플랫폼**: Windows 전용 (macOS/Linux 미지원, 크로스 플랫폼 코드 금지)

---

## 발견된 주요 버그 및 위험 (심각도순)

### 심각도: 높

| # | 항목 | 발견 팀 | 상태 |
|---|------|---------|------|
| 1 | **좀비 프로세스 포트 점유** | 엔진 | **수정 완료** — `_ensure_port_free()` 추가, 서버 시작 전 포트 점유 PID 정리 |
| 2 | **Git merge conflict 무한 대기** | 클라 | **수정 완료** — conflict/로컬변경 구분 안내 + 재시도 대기 메시지 |
| 3 | **config.json 타입 미검증** | QA | **수정 완료** — poll_interval int 변환(최소 10초), server_port int 변환 |
| 4 | **한글 경로 + CP949 인코딩 크래시** | QA | **수정 완료** — .watch_state read_text/write_text에 encoding='utf-8' 명시 |
| 5 | **초기 캐시 부재** | 클라 | **대응 완료** — cache_context MCP 도구로 에이전트가 캐시 축적 |

### 심각도: 중

| # | 항목 | 발견 팀 | 상태 |
|---|------|---------|------|
| 6 | 벡터 인덱스 경쟁 조건 | QA | **해당 없음** — ThreadPoolExecutor 완료 후 인덱싱 호출, 경쟁 조건 없음 |
| 7 | 크로스 플랫폼 분기 | 엔진 | **수정 완료** — sys.platform 체크 5곳 제거, Windows 전용 확정 |
| 8 | Silent fail 패턴 | 엔진/QA | **수정 완료** — traceback 포함 로깅, print→log 전환 |
| 9 | HTTP 타임아웃 불일치 | 엔진 | **수정 완료** — HTTP_TIMEOUT_SHORT/HTTP_TIMEOUT_INDEXING/SUBPROCESS_TIMEOUT 상수 통일 |
| 10 | LLM 폴백 비용 증가 | TD | **수정 완료** — 폴백 전 10초 대기(rate limit 대응) + 누적 폴백 횟수 로깅 |
| 11 | MCP 서버 추가 시 다중 수정점 | 엔진 | **수정 완료** — `_MCP_PROCESS_NAMES`를 agent_templates.MCP_SERVERS에서 자동 추출 (3곳→2곳) |
| 12 | config.json 입력값 미검증 | 클라 | **수정 완료** — #3과 함께 처리 |

### 심각도: 낮

| # | 항목 | 발견 팀 | 상태 |
|---|------|---------|------|
| 13 | 로그 파일 이름 패턴 불일치 시 삭제 안됨 | QA | **수정 완료** — YYYY-MM-DD 정규식으로 모든 .log 파일 정리 |
| 14 | 설정 스키마 버전 관리 없음 | 엔진 | **수정 완료** — CONFIG_VERSION + _CONFIG_DEFAULTS로 누락 필드 자동 보충 |
| 15 | 원격 서버 URL 형식 미검증 | 클라 | **수정 완료** — http:// 프로토콜 자동 추가 |

---

## 확장성 평가

| 팀 규모 | 권장 모드 | 주요 병목 | 대응 필요 |
|---------|-----------|-----------|-----------|
| **6~8명** | 로컬 | 저사양 PC 벡터 인덱싱 지연 | 현재 구조로 충분 |
| **20명** | 서버 | FastAPI 단일 워커, rebuild 중 검색 마비 | 다중 워커 필요 |
| **50명** | 서버 | HTTP 504 에러, 브랜치별 서버 필요 | 아키텍처 재설계 필요 |

---

## ROI 요약 (TD 평가)

| 항목 | 연간 |
|------|------|
| 예상 절감 (코드 리뷰 + 온보딩 + 에셋) | ~$60,000 |
| 예상 비용 (LLM + 인프라 + 유지보수) | ~$45,000~60,000 |
| **순이익** | **$0~15,000** |

6~8명 팀에서 손익분기, 20명+ 팀에서 ROI 개선. 핵심 가치는 **코드 리뷰 자동화**.

---

## 다음 분기 로드맵 (TD 권고)

1. **LLM 비용 제어** — 토큰 모니터링, 폴백 최적화, 초기 빌드 최적화
2. **서버 모드 안정화** — 다중 워커, 헬스 체크, 재구축 무중단화
3. **유지보수성 개선** — 에이전트 설정 외부화, 동적 도메인, 통합 테스트
