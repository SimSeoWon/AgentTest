# 2026-03-28 13:04 — 프로젝트 초기 구성

## 작업 개요
AgentTest 저장소 초기 설계 및 전체 기반 구조 구현.

---

## 신규 생성 파일

### 핵심 스크립트
- `watcher/watch.py` — Git 폴링 워처 메인 스크립트
- `watcher/agent_templates.py` — 에이전트/MCP 템플릿 상수 모음
- `build.bat` — 전체 빌드 스크립트 (watch.exe + MCP 3종)

### MCP 서버
- `mcp/context_search/server.py` — 태그 기반 컨텍스트 검색 (`search_context`, `list_tags`)
- `mcp/log_analyzer/server.py` — UE5 로그 분석 (`analyze_log`, `search_log`)
- `mcp/crash_analyzer/server.py` — UE5 크래시 분석 (`analyze_crash`, `analyze_crash_log`)

### 문서
- `CLAUDE.md` — 프로젝트 가이드 (초기 작성 및 전체 갱신)
- `history/` 폴더 생성

---

## 주요 설계 결정

| 항목 | 결정 내용 |
|------|----------|
| 배포 방식 | `dist/` 폴더 전체 배포 (watch.exe + mcp/*.exe) |
| 실행 환경 | PyInstaller 단일 exe — Python 불필요, Git + Claude CLI 필요 |
| Git 탐색 | `Source/.git` 1순위 (UE5 표준 구조 대응) |
| 컨텍스트 위치 | `.claude/context/` — 에이전트와 같은 `.claude/` 하위 통합 |
| 에이전트 격리 | 에이전트별 `settings.json`으로 allowedTools + mcpServers 분리 |
| Claude 호출 | `claude -p ... --dangerously-skip-permissions` (법인 라이센스 전제) |

---

## 에이전트 목록 (9개)

| 번호 | 이름 | 역할 |
|------|------|------|
| 01 | 소스분석 | 변경 파일 → RAG 컨텍스트 MD 자동 생성 |
| 02 | 프로젝트분석 | 전체 구조·의존성 분석 |
| 03 | 코드규약 | UE5 코딩 컨벤션 준수 검토 |
| 04 | 코드작성 | 컨텍스트 기반 코드 초안 생성 |
| 05 | 코드검증 | 버그·안전성 정적 분석 |
| 06 | 빌드_통합 | .Build.cs 등 빌드 파일 변경 영향 분석 |
| 07 | 코드매니저 | 전체 에이전트 조율·통합 리포트 |
| 08 | 로그분석 | UE5 .log 파일 에러/경고 분석 |
| 09 | 크래시분석 | .dmp / CrashContext.xml / 크래시 로그 분석 |

---

## 배포 후 구조 요약

```
[UE5 프로젝트 루트]/
├── watch.exe
├── mcp/context_search.exe
├── mcp/log_analyzer.exe
├── mcp/crash_analyzer.exe
├── config.json          (최초 실행 시 자동 생성)
├── .watch_state         (자동 생성)
└── .claude/
    ├── context/         (10개 UE5 도메인 폴더)
    └── agents/          (9개 에이전트 폴더, 각 role/prompt/settings)
```
